get_filename_component(SAMRAI_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(SAMRAI_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

if (NOT TARGET SAMRAI_algs AND NOT TARGET SAMRAI_appu AND NOT TARGET
    SAMRAI_geom AND NOT TARGET SAMRAI_hier AND NOT TARGET SAMRAI_math AND NOT TARGET
    SAMRAI_mesh AND NOT TARGET SAMRAI_pdat AND NOT TARGET SAMRAI_solv AND NOT TARGET
    SAMRAI_tbox AND NOT TARGET SAMRAI_xfer)

  include("${SAMRAI_CMAKE_DIR}/SAMRAITargets.cmake")

endif()

set(SAMRAI_LIBRARIES SAMRAI_algs SAMRAI_appu SAMRAI_geom SAMRAI_hier
    SAMRAI_math SAMRAI_mesh SAMRAI_pdat SAMRAI_solv SAMRAI_tbox
    SAMRAI_xfer)
